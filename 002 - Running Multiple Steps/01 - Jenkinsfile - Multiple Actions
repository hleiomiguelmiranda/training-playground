pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
       stage('build') {
            steps {
                sh 'echo $(date)'
		sh 'sleep 10s'
	   }
        }
    }
}
