pipeline {
    agent { 
    dockerfile {
            dir '003 - Defining Execution Environments'
            filename '003 - Defining Execution Environments/03 - Dockerfile - Install Software Dockerfile'
        }
    }
    stages {
        stage('build') {
            steps {
                sh "echo 'You need to install curl'"
                sh "curl www.google.com"
            }
        }
    }
}
